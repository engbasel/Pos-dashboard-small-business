import 'package:flutter/material.dart';

class CatigorysViwe extends StatelessWidget {
  const CatigorysViwe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
